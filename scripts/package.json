{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "score:stt": "tsx ./src/stt-score.ts",
    "import:vapi": "tsx ./src/stt-evals.ts import",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "cli": "tsx ./src/stt-evals.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@workspace/api-client": "workspace:*",
    "@workspace/scoring": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "vitest": "^3.2.7"
  }
}
